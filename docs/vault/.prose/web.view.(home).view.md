Página home (`/`) de la terminal. Es un wrapper mínimo `force-dynamic` que renderiza el componente `HomeView` (la landing / dashboard de entrada del producto).

Conecta con: componente `HomeView`; vive bajo el layout raíz.
