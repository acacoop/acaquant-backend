Vista `/operadores` — lente comercial por operador (cartera de clientes, actividad, estado comercial). Wrapper `force-dynamic` que delega en `OperadoresView`.

Conecta con: componente `OperadoresView` → backend Tablero Comercial (`/api/manager/comercial`, service `comercial`). Vive bajo el layout raíz.
