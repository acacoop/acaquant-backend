.env
/.env
.env.*
!.env.example
venv/
__pycache__/
*.log
logs/
Excel/response_cuenta_*.json
*.pyc
.DS_Store
.claude/*
!.claude/commands/
!.claude/skills/
!.claude/agents/
!.claude/hooks/
!.claude/settings.json
!.claude/INDEX.md
.ipynb_checkpoints/
.idea/
.pytest_cache/
.ruff_cache/
.venv/
archive/
exports/
scripts/out_1816/
scripts/out_movdocs/

# CSVs en docs/ — usados para data sensible (backfill manual de AuM, etc.)
# Nunca van al repo. Si querés versionar uno específico, forzar con `git add -f`.
docs/*.csv

# Excels de precios manuales (docs/<MMDD>.xlsx) — input local de fix_precios_aum.
docs/*.xlsx

# Material de lectura / PDFs sueltos — no van al repo.
*.pdf
security_audit_report.txt
colecciones_baseline.json
evals/_borrador_desde_flags.json

# Muestras crudas de las APIs de Interbanking (scripts/diag_interbanking_raw).
# Tienen CUIT, CBU, importes y nombres de comitentes REALES — nunca al repo.
interbanking_muestras/
