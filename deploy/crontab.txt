# Crontab de producción — TradingAV
# Fuente de verdad. Aplicar con:   crontab /root/TradingAV/deploy/crontab.txt
#
# Todos los jobs Python se invocan vía deploy/run_job.sh <nombre> <timeout> '<cmd>'
# que agrega LOCK (no se apila si ya corre) + TIMEOUT (lo mata si se cuelga) + LOG.
# Esto previene el incidente de CPU del 2026-06-03 (jobs colgados/duplicados).
# El redirect al log lo hace run_job.sh → el comando interno NO lleva >> log 2>&1.

# ─── Auto-control de CALIDAD DE DATOS (media rueda, L-V)
# Controles de CONTENIDO: bonos fuera de forwards, RF sin tasa, assets sin
# cartera, comitentes sin nivel_1, contrapartes sin alta (conciliador Aunesa).
# Diffea contra manager.controles_datos → marca SOLO lo nuevo/resuelto (detalle
# en Manager → controles). Con ANTHROPIC_API_KEY agrega lectura ejecutiva AI.
30 16 * * 1-5 /root/TradingAV/deploy/run_job.sh controles_datos 5m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.controles_datos'

# ─── Retención de tablas de log/auditoría (03:20 UTC = 00:20 ART, todos los días)
# Postgres no tiene TTL nativo: `sql/schema.sql` prometía retención (job_runs 60d,
# ia.trazas) pero el cron que la aplica nunca existió → bloat
# monótono contra el techo de storage de Supabase. Este job lo aplica: DELETE
# BATCHEADO (5000 filas/lote) + sleep entre lotes + idempotente. FUERA DE RUEDA a
# propósito (madrugada) → no compite con los motores. Ver impacto antes de borrar:
# python -m jobs.cleanup_retencion --dry-run
20 3 * * * /root/TradingAV/deploy/run_job.sh cleanup_retencion 30m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.cleanup_retencion'

# ─── Limpieza de instrumentos vencidos (12:30 UTC = 09:30 ART, L-V, antes de abrir motores)
30 12 * * 1-5 /root/TradingAV/deploy/run_job.sh cleanup_curvas 10m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.cleanup_curvas'
30 12 * * 1-5 /root/TradingAV/deploy/run_job.sh cleanup_futuros_dlr 10m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.cleanup_futuros_dlr'

# ─── Motores de mercado (Lunes a Viernes, 13:20 UTC → 20:05 UTC = 10:20 → 17:05 ART)
# Arrancan 10 min antes de la apertura (10:30 ART). Antes arrancaban 13:00 UTC y
# durante 30 min pre-apertura pisaban la foto de ayer con last=0 → tablas de mercado
# vacías a la mañana ("SIN DATOS — MERCADO CERRADO"). Con 13:20 la foto del cierre
# anterior sobrevive hasta casi la apertura. Ver memoria project_motores_arranque_preapertura.
# Nota: `restart` (no `start`) → mata cualquier instancia zombie heredada (WS muerto
# pero proceso "active"). Ver memoria project_motors_overnight_atlas_pause.
20 13 * * 1-5 systemctl restart motor_rofex.service
5 20 * * 1-5 systemctl stop    motor_rofex.service
20 13 * * 1-5 systemctl restart motor_options.service
5 20 * * 1-5 systemctl stop    motor_options.service
20 13 * * 1-5 systemctl restart motor_curvas.service
5 20 * * 1-5 systemctl stop    motor_curvas.service
20 13 * * 1-5 systemctl restart motor_forwards.service
5 20 * * 1-5 systemctl stop    motor_forwards.service
20 13 * * 1-5 systemctl restart motor_breakevens.service
5 20 * * 1-5 systemctl stop    motor_breakevens.service
20 13 * * 1-5 systemctl restart motor_caucion.service
5 20 * * 1-5 systemctl stop    motor_caucion.service
20 13 * * 1-5 systemctl restart motor_futuros_dlr.service
5 20 * * 1-5 systemctl stop    motor_futuros_dlr.service
20 13 * * 1-5 systemctl restart motor_dolares.service
5 20 * * 1-5 systemctl stop    motor_dolares.service
20 13 * * 1-5 systemctl restart motor_cedears.service
5 20 * * 1-5 systemctl stop    motor_cedears.service
20 13 * * 1-5 systemctl restart motor_agro.service
5 20 * * 1-5 systemctl stop    motor_agro.service
20 13 * * 1-5 systemctl restart motor_agro_opciones.service
5 20 * * 1-5 systemctl stop    motor_agro_opciones.service
20 13 * * 1-5 systemctl restart motor_portfolio_snapshot.service
5 20 * * 1-5 systemctl stop    motor_portfolio_snapshot.service
20 13 * * 1-5 systemctl restart motor_estrategia.service
5 20 * * 1-5 systemctl stop    motor_estrategia.service

# ─── ESTRATEGIA QUANT — resolver de señales (docs/ESTRATEGIA_QUANT.md)
# Corre INTRADÍA a propósito: el tape cedears_time_sales se VACÍA al cierre —
# si el resolver esperara a la noche, el resultado de la señal sería imposible
# de medir. --cierre a las 20:10 etiqueta parciales antes de perder el tape.
*/5 13-20 * * 1-5 /root/TradingAV/deploy/run_job.sh estrategia_resolver 4m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.estrategia_resolver'
10 20 * * 1-5 /root/TradingAV/deploy/run_job.sh estrategia_resolver_cierre 5m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.estrategia_resolver --cierre'

# ─── Motor de órdenes (10:30 ART → 17:05 ART = 13:30 UTC → 20:05 UTC)
30 13 * * 1-5 systemctl restart motor_ordenes.service
5  20 * * 1-5 systemctl stop    motor_ordenes.service

# ─── Discovery de cuentas ELIMINADO (decomiso 2026-06-29). No se descubre nada al broker:
# las cuentas para operar = espejo de las comitentes de SQL clientes.cuentas (las mantiene
# jobs.sync_comitentes, abajo). risk.listado_cuentas lee clientes.cuentas directo. Sin Mongo.

# ─── Volatilidad GGAL (al cierre)
0 20 * * 1-5 /root/TradingAV/deploy/run_job.sh volatilidad_ggal 15m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.volatilidad_ggal'

# ─── Precios acciones daily (post cierre US, 22 UTC = 19 ART, L-V)
0 22 * * 1-5 /root/TradingAV/deploy/run_job.sh precios_acciones_daily 30m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.precios_acciones_daily'

# ─── ADR live (Finnhub /quote, cada 15 min en hs US, L-V) 13:30-20:00 UTC
*/15 13-20 * * 1-5 /root/TradingAV/deploy/run_job.sh adr_live 5m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.adr_live'

# ─── Dólar MEP + CCL (cada 15 min durante rueda, L-V 13:00-20:45 UTC)
*/15 13-20 * * 1-5 /root/TradingAV/deploy/run_job.sh dolar_mep 5m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m engines.dolar_mep'

# ─── Indicadores macro argentinadatos.com (1x/día, 12:00 UTC = 09:00 ART)
0 12 * * * /root/TradingAV/deploy/run_job.sh argentina_datos 15m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.argentina_datos'

# ─── CashFlow (martes a sábado 02:00 UTC = lunes-viernes 23:00 ART).
0 2 * * 2-6 /root/TradingAV/deploy/run_job.sh cashflow 30m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.cashflow --today'

# ─── Flujo Contrapartes — ELIMINADO (decomiso 2026-06-29): el job + CashFlow.Flujo (Mongo)
#     se retiraron. La vista "flujo de contrapartes" lee operaciones.operaciones (SQL).

# ─── Negocio del día (cada 30 min 14-22 UTC = 11-19 ART L-V) → negocio_movimientos + aranceles + fci_bilateral
# Pega a Aunesa consolidadosGenerales → CashFlow.NegocioMovimientos (idempotente);
# aranceles rellena `arancel`; fci_bilateral lleva el FCI bilateral a Operaciones.
#
# REACTIVADO 2026-06-03 — `fci_bilateral` ahora acota la lectura a los últimos 10
# días (usa índice fecha_categoria, ya no escanea toda NegocioMovimientos) y el
# wrapper le da timeout=25m de red. Era la causa del CPU 100% cada hora.
#
# LOOKBACK 2026-07-28 — `negocio_movimientos` (sin args) ingiere hoy + los últimos
# días hábiles (const _LOOKBACK_HABILES). Las diferencias diarias de futuros llegan
# a Aunesa T+1 → ingerir SÓLO "hoy" las perdía (corte del 2026-05-06). Idempotente.
# ops_agregado: mantiene el agregado HOT/COLD de operaciones (días cerrados
# pre-agregados; recomputa solo días sucios via ingestado_en → barato si no hay
# cambios). Cada hora en rueda para que una corrección histórica intradía se
# re-agregue en <1h; la serie de MOVIMIENTOS lee de acá + HOY en vivo.
25 14-22 * * 1-5 /root/TradingAV/deploy/run_job.sh ops_agregado 10m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.ops_agregado'

# ops_tasa_mav va ÚLTIMO de la cadena a propósito: extrae la tasa de los boletos
# MAV desde `negocio_movimientos.informacion`, así que necesita que
# negocio_movimientos ya haya escrito el texto. Rellena sólo lo que tiene tasa
# NULL (idempotente, ventana de 7 días).
*/30 14-22 * * 1-5 /root/TradingAV/deploy/run_job.sh negocio_chain 25m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.negocio_movimientos && /root/TradingAV/venv/bin/python -m jobs.aranceles && /root/TradingAV/venv/bin/python -m jobs.fci_bilateral && /root/TradingAV/venv/bin/python -m jobs.ops_tasa_mav'

# ─── Tesorería → FOTO diaria de la grilla BANCOS (23:50 ART = 02:50 UTC del día
# siguiente, por eso los días son 2-6 = martes a sábado UTC pero L-V en Argentina).
# La vista del día es live contra Aunesa y no se persiste: esto congela, al cierre,
# las filas de BANCOS + los movimientos que componen cada celda, para poder auditar
# un día pasado. UNA foto por fecha (re-correrlo la actualiza) y TTL de 30 fechas.
# Rellenar días viejos: python -m jobs.tesoreria_snapshot --fecha 2026-08-05
50 2 * * 2-6 /root/TradingAV/deploy/run_job.sh tesoreria_snapshot 10m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.tesoreria_snapshot'

# ─── Tesorería → espejo de DEPÓSITOS de cheque en CHEQUES → RECIBIDOS
# Cada 30 min de 12 a 21 UTC = 9:00 a 18:30 ART, L-V. La ventana es por cómo
# trabaja el back office: los depósitos se cargan en Aunesa A LA MAÑANA SIGUIENTE
# con la fecha del día anterior (el 11/08 entran los fechados 10/08) y la plata
# llega al banco ese mismo día. Por eso mira los últimos 3 días hábiles, no solo
# hoy — es el mismo motivo por el que jobs/cashflow.py perdía los depósitos.
#
# TERMINABA 14:30 ART y se estiró a 18:30 (2026-08-12). El corte temprano y el
# lookback de 2 días hábiles se tocan: un depósito fechado D solo se puede
# espejar en las corridas de D y D+1, así que lo que el back office cargaba en
# Aunesa DESPUÉS de las 14:30 de D+1 ya no lo alcanzaba ninguna corrida — el día
# D nunca se volvía a mirar. Con el corte a las 18:30 el día D queda cubierto
# hasta el final de D+1, que es lo que el lookback asume.
#
# Las filas nacen SIN banco y en `pendiente`: no tocan el saldo hasta que el back
# office les asigna la cuenta operativa y las finaliza. Espeja SOLO e-cheq: el
# "Depósito de cheques" de papel es un lote y duplicaría el ingreso.
# Ver qué haría sin escribir: python -m jobs.tesoreria_echeq_recibidos --dry
# Contrastar Aunesa vs. lo cargado en la app (read-only, antes de forzar --fecha):
#   python -m scripts.diag_echeq_recibidos_vs_aunesa
*/30 12-21 * * 1-5 /root/TradingAV/deploy/run_job.sh tesoreria_echeq_recibidos 10m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.tesoreria_echeq_recibidos'

# ─── Operaciones desde /informes (cada 30 min, 13:30-22:00 UTC L-V)
# Fuente de verdad de operaciones (por CONCERTACIÓN). Idempotente + lookback.
#
# REACTIVADO 2026-06-03 — ya con run_job.sh (lock+timeout 25m) + los rollups
# puestos, la ingesta intra-día es segura. Estuvo pausada unas horas por el
# incidente de CPU; al reactivar recupera (idempotente + lookback). Recuperar un
# día puntual a mano: python -m jobs.operaciones_informes --desde Y-M-D --hasta Y-M-D
30 13-21 * * 1-5 /root/TradingAV/deploy/run_job.sh operaciones_informes 25m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.operaciones_informes'
0  14-22 * * 1-5 /root/TradingAV/deploy/run_job.sh operaciones_informes 25m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.operaciones_informes'

# ─── Cache-warming vista COMERCIAL (cada 4 min en horario de mercado L-V)
# Timeout 3m (< intervalo de 4m) + lock → no se apila si la API está lenta.
*/4 13-20 * * 1-5 /root/TradingAV/deploy/run_job.sh comercial_warm 3m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.comercial_warm'

# ─── Snapshot mensual de cuentas activas (22:30 UTC = 19:30 ART, L-V)
30 22 * * 1-5 /root/TradingAV/deploy/run_job.sh actividad_mensual 15m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.actividad_mensual'

# ─── BCRA (CER/TAMAR/DOLAR/BADLAR, 22:00 UTC L-V = 19:00 ART)
0 22 * * 1-5 /root/TradingAV/deploy/run_job.sh bcra 15m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.bcra --today'

# ─── Tenencia SQL diaria (portafolio.tenencia) — 11:00 UTC = 08:00 ART, L-V.
# Writer diario del dato SQL corregido: snapshotea el día hábil ANTERIOR (igual regla H1)
# y setea la columna `aum` al insertar. Es la fuente de /aum y /valuaciones en SQL.
# Corre ANTES del AuM Mongo (12:00) para no doblar la carga a Aunesa en simultáneo.
0 11 * * 1-5 /root/TradingAV/deploy/run_job.sh portafolio_diario 25m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.portafolio_backfill --diario'

# ─── Autocompletado del catálogo de títulos (11:40 UTC L-V, post writer diario)
# El writer de arriba da de alta las unidades NUEVAS sin metadata; este job les
# completa lo que es derivable de la propia `unidad` (FINANCIAMIENTO: cartera +
# ticker + vencimiento; FCI: cartera + código CAFCI + nombre). Solo escribe campos
# VACÍOS: si la regla discrepa de lo cargado a mano lo reporta como conflicto en
# el log y NO pisa. Ver qué haría antes de aplicar:
# python -m jobs.assets_autofill --dry
40 11 * * 1-5 /root/TradingAV/deploy/run_job.sh assets_autofill 10m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.assets_autofill'

# AuM Mongo (jobs.aum) ELIMINADO 2026-06-15: la fuente única de tenencias es SQL
# portafolio.tenencia (writer diario de arriba). jobs/aum.py quedó solo como librería
# de helpers Aunesa (autenticar/consultar_posicion/procesar/_calcular_valuacion).

# ─── Consolidado por cuenta (12:30 UTC = 09:30 ART, L-V — post AuM)
30 12 * * 1-5 /root/TradingAV/deploy/run_job.sh consolidado_cuentas 25m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.consolidado_cuentas'

# tenencia_hd ELIMINADO 2026-06-23: el job escribía Valuaciones.TenenciaHD (Mongo), un
# rollup que ya NO lee nadie — la vista Back Office Tenencia Valorizada lee SQL
# (api/services/tenencia_hd.py → portafolio.tenencia) directo. Colección dropeada.

# ─── Acreencias clientes (12:45 UTC L-V — post AuM) → CashFlow.Acreencias.
# Proyecta cobros futuros por cliente (tenencias × calendario contractual). Liviano.
45 12 * * 1-5 /root/TradingAV/deploy/run_job.sh acreencias 15m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.acreencias --commit'

# ─── PnL TOTALES precompute (cada 30 min en la rueda — :05 y :35, 15-22 UTC L-V)
# Timeout 25m (< intervalo 30m) + lock → no se apila el cómputo pesado de ~883 cuentas.
5,35 15-22 * * 1-5 /root/TradingAV/deploy/run_job.sh pnl_totales_precompute 25m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.pnl_totales_precompute'

# ops_rollup (CashFlow.OpsSerieDiaria) ELIMINADO: las series /ops/serie+aranceles
# se agregan EN VIVO desde SQL operaciones.operaciones (operaciones_sql) — sin
# precompute Mongo (migración Operaciones→SQL).

# comercial_rollup (Clientes.ComercialCache) ELIMINADO: el informe comercial agrega
# en vivo desde SQL (comercial_sql) — sin precompute Mongo (migración NegocioMovimientos→SQL).

# ─── Rollup diario de opciones (20:15 UTC = 17:15 ART, L-V)
15 20 * * 1-5 /root/TradingAV/deploy/run_job.sh options_rollup 15m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.options_rollup'

# ─── Archivo + purga de Opciones.Data (20:50 UTC = 17:50 ART, L-V — post rollup 20:15)
# Backup a JSON local + borra ticks < hoy ART de Mongo Y del espejo SQL mercado.options_data.
# DEBE correr DESPUÉS de options_rollup (que consume los ticks del día para DataHistorica).
# Sin este cron la colección/tabla acumulaban vencimientos viejos (bloat). Default es dry-run
# → el cron pasa --apply para ejecutar de verdad.
50 20 * * 1-5 /root/TradingAV/deploy/run_job.sh archive_options_data 15m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.archive_options_data --apply'

# ─── Z-score de forwards (20:30 UTC = 17:30 ART, L-V — post-cierre motor)
30 20 * * 1-5 /root/TradingAV/deploy/run_job.sh forwards_zscore 10m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.forwards_zscore'

# ─── Fair Value cierre + fit + residuos (20:25 UTC = 17:25 ART, L-V) → snapshot_cierre + fair_value
25 20 * * 1-5 /root/TradingAV/deploy/run_job.sh cierre_chain 25m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.snapshot_cierre && /root/TradingAV/venv/bin/python -m jobs.fair_value'

# ─── Cierre diario de canje (20:35 UTC = 17:35 ART, L-V — post-cierre motor)
35 20 * * 1-5 /root/TradingAV/deploy/run_job.sh cierre_canje 10m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.cierre_canje'

# ─── Snapshot diario de sintéticos (20:40 UTC = 17:40 ART, L-V — post-cierre motor)
# Materializa Trading.SnapshotsSinteticos (TNA/TE/precios de LECAP+Rofex y DLK+Rofex)
# para acumular serie histórica. Lee get_sinteticos() del último estado del día.
40 20 * * 1-5 /root/TradingAV/deploy/run_job.sh snapshot_sinteticos 10m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.snapshot_sinteticos'

# ─── Stats diarias de scalping por CEDEAR (20:06 UTC, L-V) — ANTES del cleanup
# Persiste vueltas/rango/flujo del día en Trading.DayTradingStats (la
# "costumbre" del papel para el TRADE LAB). Debe correr con el tape aún vivo:
# motor para 20:05, cleanup borra 20:10 — no mover sin mover los tres.
6 20 * * 1-5 /root/TradingAV/deploy/run_job.sh day_trading_stats 3m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.day_trading_stats'

# OHLC diario de cada CEDEAR → mercado.cedears_ohlc_daily (para pivots sobre el
# CEDEAR + ATR-20 en ARS). Lee el SNAPSHOT (no el tape), así que no le afecta el
# cleanup; corre con el motor ya parado (20:05) → valores finales del día. Ventana
# móvil 60 ruedas (≥21 para el ATR-20).
15 20 * * 1-5 /root/TradingAV/deploy/run_job.sh cedears_ohlc_daily 3m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.cedears_ohlc_daily'

# Archivo de barras de 1 MINUTO por CEDEAR → mercado.cedears_bars_1m. Resamplea el
# tape (cedears_time_sales, que se VACÍA al cierre) a OHLCV/min y lo guarda para
# siempre (fuente de verdad del Efficiency Ratio intradía). Corre con el motor ya
# parado (20:05) y ANTES del cleanup del tape (23:50). Ventana móvil 60 ruedas.
20 20 * * 1-5 /root/TradingAV/deploy/run_job.sh cedears_bars_1m 3m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.cedears_bars_1m'

# OHLC diario de cada BONO (renta fija) → mercado.bonos_ohlc_daily (para pivots
# sobre el bono en la vista TRADING). Lee mercado.market_snapshot (que escribe
# motor_rofex), con el motor ya parado (20:05) → valores finales del día. Ventana
# móvil 20 ruedas. Gemelo de cedears_ohlc_daily.
16 20 * * 1-5 /root/TradingAV/deploy/run_job.sh bonos_ohlc_daily 3m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.bonos_ohlc_daily'

# ─── Vaciado del Time & Sales intradía de CEDEARs (23:50 UTC = 20:50 ART, L-V — tarde del MISMO día)
# mercado.cedears_time_sales es intradía (tape del scanner). ANTES se borraba
# 5 min después del cierre (20:10 UTC) y la mesa perdía el tape/chart al toque
# (pedido 2026-07-14): ahora se borra a las 20:50 ART del mismo día — la rueda
# queda disponible para revisar de 17:05 a 20:50, y se limpia justo antes de
# que rote la ventana "solo hoy" del reader (21:00 ART = 00:00 UTC).
# day_trading_stats (20:06, destila el día) sigue corriendo ANTES del borrado.
50 23 * * 1-5 /root/TradingAV/deploy/run_job.sh cleanup_cedears_timesales 5m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.cleanup_cedears_timesales'

# ─── Atlas cluster pause/resume ELIMINADO (decomiso Mongo 2026-06-29): Mongo vacío, no se
# pausa/resumea más. El M10 se pausa/cancela A MANO una sola vez (tras limpiar el código).

# ─── News RSS medios AR — 1er tiro 11:35 UTC (post-resume 11:30) + cada 15 min 12-23 UTC (todos los días)
35 11    * * *   /root/TradingAV/deploy/run_job.sh news_ingesta 5m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.news_ingesta'
*/15 12-23 * * * /root/TradingAV/deploy/run_job.sh news_ingesta 5m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.news_ingesta'

# ─── News Finnhub (globales + ADRs AR/LATAM) — 1er tiro post-resume 11:30 + cada 30 min 12-23 UTC
35 11    * * *   /root/TradingAV/deploy/run_job.sh news_finnhub 10m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.news_finnhub'
*/30 12-23 * * * /root/TradingAV/deploy/run_job.sh news_finnhub 10m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.news_finnhub'

# ─── Market quotes (watchlist HOME / vista GENERAL) — cada 1 min.
# Los futuros globales (ES/NQ/CL/cripto) cotizan casi 24h → corre la ventana
# 07:00-23:00 ART (10-23 UTC L-V + 0-1 UTC Mar-Sáb). Índices/Treasury fuera de
# hs devuelven su último cierre (sin daño). Timeout 50s (< intervalo 60s).
*    10-23 * * 1-5 /root/TradingAV/deploy/run_job.sh market_quotes 50s 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.market_quotes'
*    0-1  * * 2-6 /root/TradingAV/deploy/run_job.sh market_quotes 50s 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.market_quotes'

# ─── Market anchors (retornos 7d/MTD/YTD/1Y) — 1x/día post-cierre US (22:00 UTC, L-V)
0 22 * * 1-5 /root/TradingAV/deploy/run_job.sh market_anchors 15m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.market_anchors'

# ─── Sync Mongo→Postgres ELIMINADO (decomiso Mongo 2026-06-29): era el bridge Mongo→SQL.
# Mongo está vacío y todos los writers son SQL-native → el bridge no tiene fuente. Job borrado.

# ─── Sync comitentes (master de clientes) — 3x/día L-V (14/17/21 UTC)
0 14 * * 1-5 /root/TradingAV/deploy/run_job.sh sync_comitentes 15m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.sync_comitentes'
0 17 * * 1-5 /root/TradingAV/deploy/run_job.sh sync_comitentes 15m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.sync_comitentes'
0 21 * * 1-5 /root/TradingAV/deploy/run_job.sh sync_comitentes 15m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.sync_comitentes'

# ─── Triage IA de incidentes (QuantAI P2) — REACTIVO: cada 10 min chequea fallas
# nuevas en job_runs (query gratis) y solo una firma NUEVA gasta un diagnóstico
# del LLM (guardas: dedup + watermark + presupuesto + severidad). docs/QUANTAI.md.
*/10 * * * * /root/TradingAV/deploy/run_job.sh triage 5m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.triage'

# ─── Control de CALIDAD de conversaciones de IA (jobs/ia_calidad.py) — cierra el
# loop que era manual. 1x/dia 21:30 UTC (post-rueda): marca los 👎 + un critico
# barato sobre los turnos con señal de falla; las flags quedan en
# ia.calidad_flags. NO auto-corrige. Solo gasta tokens en los candidatos que
# pasan el pre-filtro deterministico; un dia sin señales = 0.
30 21 * * 1-5 /root/TradingAV/deploy/run_job.sh ia_calidad 10m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.ia_calidad'

# ─── Research diario por mail (QuantAI P6) — ingesta IMAP + destilado LLM → ia.research
# Cada 30 min a la mañana L-V (el mail llega temprano ART): dedup por Message-ID
# → re-correr no duplica ni re-gasta tokens; un run sin mail nuevo = 0 tokens.
# Requiere RESEARCH_IMAP_USER/PASSWORD/MAIL_FROM en el .env. docs/QUANTAI.md.
*/30 10-14 * * 1-5 /root/TradingAV/deploy/run_job.sh research_mail 5m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.research_mail'

# ─── Guardrails de datos (invariantes post-cierre) — docs/OBSERVABILIDAD_ROBUSTEZ.md
# Corre DESPUÉS de snapshot_cierre (20:25). Report en el log del job + stat
# `violaciones` en manager.job_runs; los umbrales se fijan en
# config.GUARDRAILS_UMBRALES (None = sin calibrar, no marca violación).
45 20 * * 1-5 /root/TradingAV/deploy/run_job.sh guardrails 10m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.guardrails'

# ─── BCRA (tab BCRA de RESEARCH) — series monetarias v4 → research.bcra_series
# 4×/día L-S (el BCRA publica 1×/día hábil con rezago 1-2d; la vista sirve de la
# DB 24/7). Incremental por watermark (−7d, captura revisiones). docs/RESEARCH_BCRA.md.
0 12,16,20,23 * * 1-6 /root/TradingAV/deploy/run_job.sh bcra_research 15m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.bcra_research'

# ─── Market Data 1816 (vista RESEARCH) — series históricas EOD → research.mkt_1816_series
# Diario post-cierre L-V (22 UTC = 19 ART). Modo diario (últimos 7 días, idempotente).
# El backfill de 1 año se corre A MANO UNA VEZ (--backfill). Feed separado de la RF.
# Requiere MERCADO_1816_API_KEY. Cliente con throttle+backoff (rate limit 429).
0 22 * * 1-5 /root/TradingAV/deploy/run_job.sh mercado_1816_series 15m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.mercado_1816_series'

# ─── FRED (tab DATOS INTERNACIONALES de RESEARCH) — series macro global → research.fred_observations
# 4×/día L-V (FRED publica en días hábiles USA; la vista sirve de la DB 24/7).
# Incremental por watermark con colchón por frecuencia (D:−10d, M/Q:−95d, revisiones).
# Requiere env FRED_API_KEY. Arranque: bloque Tasas USA. docs/RESEARCH_FRED.md.
0 12,16,20,23 * * 1-5 /root/TradingAV/deploy/run_job.sh fred_research 15m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.fred_research'

# ─── Cierres diarios de los feeds Eikon (bonos offshore + Chicago) → anchors 7D/MTD/YTD
# 21:10 UTC L-V (post cierre NY 20:00 UTC y rueda diurna CBOT). Solo persiste si el feed corrió HOY.
10 21 * * 1-5 /root/TradingAV/deploy/run_job.sh eikon_cierres 5m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.eikon_cierres'

# ─── TENENCIA LIVE (posición T0/T1 del día) → portafolio.tenencia_live
# Daemon con loop propio, arrancado/parado por cron igual que los motores.
# 11:00 UTC = 8:00 ART L-V. NO pisa al backfill diario (que arranca a la misma
# hora): el daemon espera a que la corrida `aum` de hoy figure terminada en
# manager.job_runs antes de su barrido de apertura. El proceso termina SOLO al
# cierre de rueda (18 ART); el stop de las 21:05 UTC es red de seguridad.
# NO toca portafolio.tenencia / assets / AuM / carteras — escribe solo su tabla.
0 11 * * 1-5 systemctl restart tenencia_live.service
5 21 * * 1-5 systemctl stop    tenencia_live.service

# ─── CONTROL DE SALDOS (saldo LIQUIDADO del día por cuenta y moneda) → portafolio.control_saldos
# Mismo diseño y mismo horario que tenencia_live, pero contra OTRO endpoint de
# Aunesa (`cuentas/{id}/posiciones`), que separa lo liquidado de lo pendiente.
# Alimenta el control de descubiertos del back office. También espera a que la
# corrida `aum` de hoy figure terminada antes del barrido de apertura.
# NO toca portafolio.tenencia / tenencia_live / assets / AuM — escribe solo su tabla.
0 11 * * 1-5 systemctl restart control_saldos.service
5 21 * * 1-5 systemctl stop    control_saldos.service

# ─── INTERBANKING (extractos de los bancos) → esquema bancos.*
# Cada 2hs de 9 a 19 ART = 12,14,16,18,20,22 UTC, L-V. En cada corrida
# re-sincroniza AYER y HOY: un movimiento de ayer puede aparecer o corregirse
# después del cierre del banco, y re-pedirlo cuesta una llamada por cuenta.
# La ingesta es idempotente (PK por hash del movimiento), así que correrla de más
# no duplica nada.
# ~35 llamadas por corrida contra un límite de 100/minuto que es del ABONADO
# (no del proceso) — por eso la vista NUNCA le pega a Interbanking en vivo.
# Doc: docs/INTERBANKING.md
0 12,14,16,18,20,22 * * 1-5 /root/TradingAV/deploy/run_job.sh interbanking_sync 15m 'cd /root/TradingAV && /root/TradingAV/venv/bin/python -m jobs.interbanking_sync'
