Generador determinista que regenera el inventario de deploy/SISTEMA.md (el plano único del sistema) desde la fuente real: deploy/systemd/*.service + deploy/crontab.txt. Reescribe solo las tablas de servicios/motores/crons entre marcadores AUTOGEN para que no puedan desincronizarse; la narrativa (topología, flujo de datos) se mantiene a mano. Hay que correrlo cada vez que se agrega/quita/modifica un servicio systemd o un cron. Con `--check` falla si quedó desincronizado. Se corre con `python -m scripts.gen_sistema [--check]`.
Conecta con: lee deploy/systemd/ y deploy/crontab.txt; genera deploy/SISTEMA.md (skill /sistema).
